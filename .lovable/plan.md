# Try It Live section

## Build
- Create a self-contained `TryItLive.tsx` React component with the exact supplied copy, light and dark variants, and no dependencies beyond React.
- Implement idle, valid, field error, submitting, calling, capacity, unsupported, and unavailable presentations in one fixed-height card.
- Add accessible labels, validation descriptions, status announcements, keyboard/focus states, mobile sizing, reduced-motion behavior, and the Turnstile/sample-call slots.
- Include the clearly marked preview-only controls for switching every state and theme.

## Preview integration
- Replace the placeholder home screen with the new section.
- Define the supplied semantic color and font-family tokens for the preview without adding other visual colors or sources.
- Add page-specific metadata and verify the section at desktop and mobile sizes.
